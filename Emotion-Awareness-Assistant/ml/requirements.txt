# requirements.txt — Python packages needed to train the FER2013 model
# Install step 1:  pip install -r ml/requirements.txt
# Install step 2:  pip install tensorflowjs==4.9.0 --no-deps

# TensorFlow 2.21.0 — only version with Python 3.13 Windows wheels
tensorflow==2.21.0

# Kaggle API for dataset download
kaggle>=1.6.0

# Data processing
numpy>=1.23.0
Pillow>=9.0.0
pandas>=1.5.0
scikit-learn>=1.2.0
matplotlib>=3.6.0
